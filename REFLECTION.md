In this assignment, I found basically nothing but success. Using Claude was much easier than I had expected, and the code that it produced functioned perfectly the entire time. I will note that I'm not entirely sure I was using Claude in the way that the assignment was intended. Admittedly, I don't understand some of the code (at least not every single line) but I do understand what each function does, why it's important, and some of the nuances of each one (like rebalancing in rb_delete and rb_insert). I didn't just one-shot the assignment by telling Claude "code a red-black tree," but I'm not sure how small the assignment expected our Claude usage to be. My "strategy" with Claude on this assignment was: break the assigment down into its key components, and every single time Claude wrote any code, run all my tests (including asan and memcheck) and ensure they report green before continuing to the next component. The thing that surprised me most is that Claude didn't introduce a single error the entire time, and asan + memcheck never reported anything but green. I was dilligent in reviewing all the responses that Claude gave me, but I failed to find anything suspiscous or anything that stood out.

I'm not able to reflect on a bug that Claude introduced as requested in deliverables, because Claude didn't end up introducing any bugs (at least, none that I or any of the tests could find).

The one thing that surprised me the most in the switch to C from Java is just how advanced Java truly is with it's memory management. The fact that Java is able to safely introduce and remove data from memory automatically really stands out to me, as it shows just how advanced coding languages have become. And to think that Java came out in 1999, I'm curious as to what advancements the newer coding languages have been able to implement.